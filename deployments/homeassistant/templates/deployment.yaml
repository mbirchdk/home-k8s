apiVersion: apps/v1
kind: Deployment
metadata:
  name: home-assistant
  namespace: homeassistant
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: home-assistant
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: home-assistant
    spec:
      hostNetwork: true
      volumes:
      - name: configfolder
        hostPath:
          path: /home/ubuntu/home-assistant
      - name: mediafolder
        hostPath:
          path: /home/ubuntu/media
15    - name: footballsensors
16      configMap:
17        name: footballsensors
      containers:
      - name: home-assistant
        image: homeassistant/home-assistant:stable
        imagePullPolicy: IfNotPresent
        securityContext:
          privileged: true
        ports:
        - containerPort: 8123
          name: http
          protocol: TCP
        volumeMounts:
        - mountPath: /config 
          name: configfolder
        - mountPath: /media 
          name: mediafolder
        - mountPath: /config/sensors/footballsensors.yaml
          name: footballsensors

      
